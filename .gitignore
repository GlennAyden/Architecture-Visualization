node_modules/
.next/
.turbo/
dist/
build/
out/
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
.convex/
# convex/_generated/{api,dataModel,server}.* is committed so Vercel can build
# without CONVEX_DEPLOY_KEY. Regenerated by `npx convex dev` / `convex deploy`.
# The ai/ subfolder (npx convex ai-files install) stays per-checkout.
convex/_generated/ai/
coverage/
.pnpm-store/
*.tsbuildinfo
.eslintcache
.prettiercache
.data/
*.sqlite
*.sqlite-*
local-auth*.pem
local-auth*.key

# Per-user Claude Code overrides; project-level .claude/settings.json + hooks stay tracked
.claude/settings.local.json

# Sprint 5 item J — post-commit hook writes unlinked-file suggestions here.
# Per-checkout, never committed: a fresh clone has nothing pending.
.arch-viz/
